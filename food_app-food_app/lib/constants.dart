import 'package:flutter/material.dart';

//all constant of the application
const primaryColor = Color(0XFFFF5722);
const iconColor = Color(0XFFFF8A65);
const primaryLightColor = Color(0xB3FFFFFF);
const primaryGradientColor = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [Color(0xFF212121), Color(0xFF9E9E9E)]);

const secondaryColor = Color(0xFF979797);
const txtColor = Color(0xFF000000);

const kcolor = Color(0xFF212121);

const animationDuration = Duration(milliseconds: 200);
